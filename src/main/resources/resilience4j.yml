resilience4j:
  retry:
    instances:
      productApi:
        max-attempts: 3
        wait-duration: 200ms

  circuitbreaker:
    instances:
      productApi:
        sliding-window-size: 20
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s